<h2>New Employee</h2>

<%= render "form.html", changeset: @changeset, departments: @departments,
                        action: employee_path(@conn, :create) %>

<span><%= link "Back", to: employee_path(@conn, :index) %></span>
