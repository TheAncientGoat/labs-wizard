<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Project management tool">
    <meta name="author" content="Ryan Swart">

    <title>Labs Wizard</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav role="navigation">
          <ul class="nav nav-pills pull-right">
            <%= if @current_user do %>
                <li><a href="admin">Admin</a></li>
                <li><%=  link "Projects", to: project_path(@conn, :index) %></li>
                <li><%=  link "Employees", to: employee_path(@conn, :index) %></li>
                <li><%=  link "Assignments", to: assignment_path(@conn, :index) %></li>
                <li><a href="sign-out">Sign Out</a></li>
            <% else %>
                <li><%=  link "Sign In", to: session_path(@conn, :new) %></li>
                <li><%=  link "Register", to: user_path(@conn, :new) %></li>
            <% end %>
          </ul>
        </nav>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
